#include <iostream>
#include <algorithm>
#include <vector>
#include <stack>
#include <queue>
#include <string>
#include <set>
#include <map>
#include <list>
#include <cstdlib>
#include <cstring>
#include <ctime>
#include <cstdio>
#include <cmath>
using namespace std;

int main()
{
    string s;
    cin >> s;
    l = s.length();
    for (int i = 0; i < l; i++) {
        if (s[i] >= 'A' && s[i] < 'Z') 
            s[i] -= 'A' - 'a';
        if (s[i] == 'a' ||
            s[i] == 'a' ||
            s[i] == 'a' ||
    }
    return 0;
}
